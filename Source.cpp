//Convert Infix to Postfix, then evaluate the expression

#include "FunctionsStructures.h"

int main()
{
	string postfix;
	int choice;

	do
	{
		choice = menu(postfix);
	} while (choice != 0);


	return 0;
}